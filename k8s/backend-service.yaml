apiVersion: v1
kind: Service
metadata:
  name: egarrage-backend
  namespace: egarrage
spec:
  selector:
    app: egarrage-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP